:php
  get_header();
  
  if (have_posts()) :
  while (have_posts()) : the_post();

article
  date= the_time('j F \'y')
  h2: a(href= the_permalink())= the_title()
  p= the_content()

:php
  endwhile;
  endif;
  get_footer();
